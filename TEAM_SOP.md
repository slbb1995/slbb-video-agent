# Team SOP

## 角色分工

- 总控负责人：只看 `workflow_state.json` 和 `_handoff/next_step.md`，负责推进状态和人工闸门。
- 剧情负责人：负责 S1。
- 图片提示词负责人：负责 S2。
- 视频提示词负责人：负责 S3。
- 平台生成执行人：负责 S4，手动在即梦、可灵等平台生成，并记录版本。
- 质检负责人：负责 S5。
- 剪辑负责人：负责 S6。
- 分发负责人：负责 S7。
- 复盘负责人：负责 S8。

## 工作原则

- 谁负责一个阶段，谁产出该阶段 artifacts。
- 总控不补专业内容，只决定下一步该跑哪个阶段。
- 每个阶段都要停在人工闸门，不允许模型替人确认。
- S2 后按片段推进，默认一个片段跑完 S3-S8 后再进入下一个片段。

## 每日交接

交接时只需要给团队这三个信息：

```text
1. run 目录路径
2. artifacts/_handoff/next_step.md
3. 当前人工闸门是否已确认
```

## 不允许做的事

- 不要跳过 validator。
- 不要在 S3 一次生成所有片段，除非负责人写明批量范围。
- 不要在 S4/S7/S8 声称已经生成、发布或拿到数据，除非有真实路径、截图、CSV 或人工记录。
- 不要把过程解释塞进主产物；过程信息放 `_meta`、`_audit` 或 `_handoff`。
