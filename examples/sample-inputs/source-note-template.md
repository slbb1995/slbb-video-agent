# Source Note Template

> **如果本 run 是从 V2 监控台（ai-drama-monitor）导入的 handoff.md**：可省略下面 8 段。S1 会直接从 `workflow_state.json.source` 读 `v2_video_url` / `matched_rules` / `manual_note` 等字段，不需要再手填 source note。下面模板仅用于**手工启动**的 run（不走 V2 入口）。

## 项目标题

## 素材来源

## 已知剧情

## 核心人物

## 目标平台

## 目标时长

## 禁止事项

## 负责人备注
